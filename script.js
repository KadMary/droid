$(function () {
    // 1. Initialize fotorama manually.
    var $fotoramaDiv = $('#fReview').fotorama();

    // 2. Get the API object.
    var fotorama = $fotoramaDiv.data('fotorama-review');
});